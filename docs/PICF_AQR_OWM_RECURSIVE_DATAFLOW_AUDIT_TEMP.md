# PICF-AQR-OWM Recursive Dataflow Audit Temp

Generated by `scripts/picf_owm_dataflow_trace.py`.

Status: PASS (17/17 dataflow nodes passed).

This document is intentionally stricter than a substring contract verifier: every node records the mathematical formula, the invariant that protects the posterior belief-state design, and the code evidence that the dataflow exists.

## 1. Observation and recurrent carry - PASS

Formula:

```text
o_t=(I_t,D_t,T_t,q_t,ell,a_{t-1}), b_{t-1}^+ -> observe_step
```

Invariant:

```text
The current update must start from previous posterior/predictive carry, not from a stateless router.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:5479: def observe_step(`
- `src/openpi/picf/core/pipeline.py:964: previous: PicfPreviousState | None`

## 2. Production default profile - PASS

Formula:

```text
default train profile = AQR(on) + PG semantic/image support + temporal V-JEPA + posterior cache scaffold + guarded OWM losses
```

Invariant:

```text
The latest OWM profile must be the default path; legacy routers and risky auxiliary losses are explicit ablations.
```

Evidence:

- `src/openpi/picf/core/config.py:143: aqr_mapg_enabled: bool = True`
- `scripts/picf_core_train.py:6060: default="paligemma",`
- `src/openpi/picf/core/training.py:71: lambda_mapg_cycle: float = 0.02`
- `src/openpi/picf/core/training.py:76: lambda_slot_jepa: float = 0.0`

## 3. V-JEPA temporal evidence - PASS

Formula:

```text
M_vjepa={z_{tau,h,w}: tau in T_recent}; p_j(tau,h,w)=softmax(q_j^T k_{tau,h,w})
```

Invariant:

```text
Production support preserves temporal slices; last_two_mean is only an ablation.
```

Evidence:

- `src/openpi/picf/vjepa/wrapper.py:62: def recent_maps(self, n: int = 2) -> torch.Tensor | np.ndarray:`
- `src/openpi/picf/core/pipeline.py:1587: fmap.recent_maps(n=recent_count),`
- `src/openpi/picf/core/pipeline.py:2740: vjepa_temporal_priors = torch.zeros((anchor_count, temporal_count), device=self.device, dtype=self.dtype) if temporal_count > 0 else None`

## 4. PaliGemma image evidence - PASS

Formula:

```text
p_pg(j,v,u)=softmax_u((Wq q_j)^T(Wk e_pg[v,u])/sqrt(d))
```

Invariant:

```text
PG image evidence must survive as graph.pg_priors and may not be destroyed into a V-JEPA bias only.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:2184: for index, (start, end) in enumerate(semantic.image_token_ranges):`
- `src/openpi/picf/core/pipeline.py:2211: pg_priors[rows] = self._aqr_competitive_support(pg_weights, eps=self.config.epsilon_a)`
- `src/openpi/picf/core/pipeline.py:2847: pg_priors=pg_priors,`

## 5. Typed token field - PASS

Formula:

```text
M_t={M_text,M_pg,M_vjepa,M_point,M_tactile,M_post,M_cache}
```

Invariant:

```text
Missing modalities are masked/empty; typed support must not be collapsed into one opaque token before AQR.
```

Evidence:

- `src/openpi/picf/core/contracts.py:103: class PicfTokenFieldState:`
- `src/openpi/picf/core/contracts.py:129: temporal_visual: PicfTemporalVisualSupportState | None = None`
- `src/openpi/picf/core/pipeline.py:2276: def _previous_evidence_cache_tokens(self, previous: PicfPreviousState | None) -> tuple[torch.Tensor, torch.Tensor | None]:`

## 6. Previous evidence cache read - PASS

Formula:

```text
w_c proportional source_factor/(1+age+uncertainty+lambda_innov*innovation_at_write); q<-q+lambda_cache*(Read_C(q)-q)
```

Invariant:

```text
A step can read only previous carry cache; read_weight scales the cache residual; current posterior writes cache for the next step only.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:2279: cache = getattr(previous.predictive, "evidence_cache", None)`
- `src/openpi/picf/core/pipeline.py:2290: innovation_cost = float(self.config.evidence_cache_innovation_downweight) * torch.clamp(innovation, min=0.0)`
- `src/openpi/picf/core/pipeline.py:2796: q_before_cache = q`
- `src/openpi/picf/core/pipeline.py:2803: q = q_before_cache + (cache_scale * (cache_read - q_before_cache))`

## 7. AQR measurement routing - PASS

Formula:

```text
l_{j,i}^{(m)}=(Wq[a_j,c_j^-,r_j])^T Wk e_i^{(m)}/sqrt(d)+biases
```

Invariant:

```text
AQR produces measurements/supports; it does not replace posterior belief.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:2637: def _build_aqr_anchor_graph(`
- `src/openpi/picf/core/pipeline.py:2458: visual_priors: torch.Tensor,`
- `src/openpi/picf/core/pipeline.py:1882: anchor_point_priors=torch.zeros((0, point_count), device=self.device, dtype=self.dtype),`
- `src/openpi/picf/core/pipeline.py:2518: tactile_priors: torch.Tensor | None,`
- `src/openpi/picf/core/pipeline.py:2561: posterior_priors: torch.Tensor | None,`

## 8. Projective point-visual geometry - PASS

Formula:

```text
L_pv = D(p_point, p_visual P_{v->p}) + D(p_visual, p_point P_{p->v})
```

Invariant:

```text
PV alignment is geometry-projected support consistency, not a cosmetic RoPE/embedding shortcut.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:460: projective_compatibility: torch.Tensor,`
- `src/openpi/picf/core/training.py:956: point_from_visual = visual @ point_given_visual.T`
- `src/openpi/picf/core/training.py:957: visual_from_point = point @ visual_given_point`
- `src/openpi/picf/core/training.py:71: lambda_mapg_cycle: float = 0.02`

## 9. Prior prediction - PASS

Formula:

```text
S_t^- = F_theta(S_{t-1}^+, a_{t-1}, proprio_t)
```

Invariant:

```text
Prior is predicted from previous posterior, previous action, and proprio before measurement correction.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:4780: def _current_prior(self, previous: PicfPreviousState | None, observation: PicfObservation) -> tuple[torch.Tensor, ...]:`
- `src/openpi/picf/core/pipeline.py:2259: post_count = int(previous.posterior.tokens.shape[0])`
- `src/openpi/picf/core/pipeline.py:3178: executed = getattr(previous.predictive, "executed_action", None)`

## 10. Posterior correction - PASS

Formula:

```text
Lambda^+=Lambda^-+Lambda_meas; eta^+=Lambda^- mu^-+Lambda_meas mu_meas; mu^+=(Lambda^+)^{-1}eta^+
```

Invariant:

```text
Posterior after correction is the authoritative current belief.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:5190: lambda_prior = 1.0 / torch.clamp(bar_var, min=self.config.sigma_min2)`
- `src/openpi/picf/core/pipeline.py:5191: eta_prior = lambda_prior * bar_mu`
- `src/openpi/picf/core/pipeline.py:5192: lambda_meas = torch.sum(beta[:, :, None] / torch.clamp(vote_var_t, min=self.config.sigma_min2), dim=0)`
- `src/openpi/picf/core/pipeline.py:5193: eta_meas = torch.sum(beta[:, :, None] * vote_mu_t / torch.clamp(vote_var_t, min=self.config.sigma_min2), dim=0)`
- `src/openpi/picf/core/pipeline.py:5195: mu_post = var_post * (eta_prior + eta_meas)`

## 11. State-only burn-in consistency - PASS

Formula:

```text
b_t^{burnin}=U_AQR(o_t,P(b_{t-1},a_{t-1})) when AQR is enabled
```

Invariant:

```text
Burn-in and train suffix must use the same measurement model.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:5705: # Keep state-only burn-in on the same AQR measurement model as the trainable suffix.`
- `src/openpi/picf/core/pipeline.py:2637: def _build_aqr_anchor_graph(`
- `src/openpi/picf/core/pipeline_test.py:1096: def test_recurrent_burnin_uses_aqr_graph_when_aqr_enabled(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:`

## 12. Innovation - PASS

Formula:

```text
nu_t = Sigma_pred^{-1/2}(y_t - yhat_t)
```

Invariant:

```text
Innovation compares real current targets against world-only prediction; it gates correction/cache trust.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:5425: def _innovation(`
- `src/openpi/picf/core/pipeline.py:3723: physical_prediction_cache = self._prediction_cache_from_global(physical_global_pred)`
- `src/openpi/picf/core/pipeline.py:974: innovation_norm: torch.Tensor`

## 13. Slot prediction targets - PASS

Formula:

```text
L_slot=||F(S_t,a_t)-stopgrad(S_{t+1}^{posterior})||
```

Invariant:

```text
Future posterior is a detached target only; it is never current action input.
```

Evidence:

- `src/openpi/picf/core/training.py:1804: target_slots = future.posterior_tokens.detach().to(device=slot_tokens.device, dtype=slot_tokens.dtype)`
- `src/openpi/picf/core/training.py:338: def future_targets_from_current_targets(`

## 14. Temporal binding consistency - PASS

Formula:

```text
L_bind = CE(cos(c_t,stopgrad(c_{t+1}))/tau, identity labels) + entropy guard
```

Invariant:

```text
Binding consistency must include temporal identity contrast, not only current binding sharpness.
```

Evidence:

- `src/openpi/picf/core/training.py:274: def _binding_consistency_loss(`
- `src/openpi/picf/core/training.py:302: future_tokens = future.posterior_tokens`
- `src/openpi/picf/core/training.py:313: forward = fn.cross_entropy(logits, labels, reduction="none")`

## 15. Evidence cache write - PASS

Formula:

```text
C_t = write(topk(evidence), S_t^+, uncertainty_t, innovation_t) for t+1
```

Invariant:

```text
Cache is written after posterior correction and is auxiliary evidence for later steps only.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:3779: def _write_evidence_cache(`
- `src/openpi/picf/core/pipeline.py:3814: slot_address = posterior.slot_address if posterior.slot_address is not None else self.posterior_slot_token.to(device=self.device, dtype=self.dtype)`
- `src/openpi/picf/core/pipeline.py:2288: innovation = cache.innovation_at_write.to(device=self.device, dtype=self.dtype)[valid]`

## 16. Action path - PASS

Formula:

```text
a_t ~ PI0.5(posterior, task anchors, innovation, typed supports)
```

Invariant:

```text
PI0.5 remains final action generator; OWM does not create a separate action head truth path.
```

Evidence:

- `src/openpi/picf/core/pipeline.py:3627: def _build_conditioned_control_state(`
- `src/openpi/picf/core/pipeline.py:1337: self.posterior_to_control_proj = nn.LazyLinear(semantic_trunk_dim)`
- `src/openpi/picf/core/pipeline.py:1339: self.innovation_to_control_proj = nn.LazyLinear(semantic_trunk_dim)`
- `src/openpi/picf/core/pipeline.py:1341: self.task_to_control_proj = nn.LazyLinear(semantic_trunk_dim)`

## 17. False-positive guards - PASS

Formula:

```text
acceptance != substring verifier; address drift is not identity; old CALVIN failure is not new-code proof
```

Invariant:

```text
Removed dead knobs/loss-looking placeholders and stale metrics that could create false confidence.
```

Evidence:

- `scripts/verify_picf_owm_contract.py:156: "Only mathematically grounded OWM loss knobs should be available; weak placeholder losses must stay removed.",`
- `scripts/verify_picf_owm_contract.py:156: "Only mathematically grounded OWM loss knobs should be available; weak placeholder losses must stay removed.",`
- `docs/PICF_AQR_OWM_FINAL_DEPLOYMENT_README.md:2165: 3. Do not expose placeholder losses for cross-modal alignment, ordinal rank, or`

## Final Interpretation

A PASS here means the current repository contains a coherent code-level dataflow for typed evidence -> AQR measurement -> posterior correction -> prediction/cache -> action.

It does not prove empirical CALVIN anchor quality. Runtime metrics and video diagnostics must be checked separately with `scripts/picf_owm_strict_diagnose.py --metrics-jsonl ... --eval-dir ...`.
